opam-version: "1"

package "uri" {
  version:    "0.3dev"
  maintainer: "anil@recoil.org"
  build: [
    [ "make" "all" ]
    [ "make" "install" ]
  ]
  remove: [ "ocamlfind" "remove" "uri" ]
  depends: [ "ocamlfind" "re" ]
}
