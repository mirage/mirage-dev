opam-version: "1"

package "dns" {
  version:    "0.3dev"
  maintainer: "anil@recoil.org"
  build: [
    [ "make" "all" ]
    [ "make" "install" ]
  ]
  remove: [ "ocamlfind" "remove" "dns" ]
  depends: [ "ocamlfind" "cstruct" "lwt" "re" "uri" ]
}
