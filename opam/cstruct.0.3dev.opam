opam-version: "1"

package "cstruct" {
  version:    "0.3dev"
  maintainer: "anil@recoil.org"
  build: [
    [ "make" "all" ]
    [ "make" "install" ]
  ]
  remove: [ "ocamlfind" "remove" "cstruct" ]
  depends: [ "ocamlfind" ]
}
